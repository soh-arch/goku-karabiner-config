# AbcAct 操作マニュアル

> **図解版(英語)**: <https://soh-arch.github.io/goku-karabiner-config/>
> 同じ配列を、キーボード図・16ティアのインタラクティブな探索・全文検索つきで説明しています。

このマニュアルは、Goku/Karabiner-Elementsの設定ファイル(`AbcAct.edn`)がどう動くかを、
技術的な仕組みを知らなくても使えるように、実際の操作と結果だけで説明したものです。

## 1. 全体の考え方

このキーボード配列には、3つの「特殊レイヤー」があります。特定のキーを押したまま別のキーを
押すことで、普段と違う動作をさせます。

| レイヤー名 | 起動キー | 主な用途 |
|---|---|---|
| **Asterisk** | 左⌘ | アプリ起動・カーソル移動・編集・ウィンドウ管理など |
| **Bra** | 左⇧ | 片手用のテンキー |
| **Cket** | 右⇧ | 音量・輝度などメディア操作 |

### Act(アクト)

**Asterisk**レイヤーの中には「Act」という第2段階の仕組みがあります。左⌘を押したまま、
ホームポジションの`a`/`s`/`d`/`f`キーを**追加で**押しっぱなしにすることで、他のキー
(`h`/`j`/`k`/`l`など)の動作を切り替えられます。`a`/`s`/`d`/`f`は同時に複数押せるので、
組み合わせは16通りあります。**この16通りを「ティア」と呼びます。**

各Actキーにはそれぞれ意味があり、どのティアで何を行うかはその組み合わせで決まります。

| Actキー | 意味 |
|---|---|
| `a` | 増幅する / 別のものに変える / アプリ(左手ブロック) |
| `s` | 選択する / 保存する(下段) |
| `d` | 削除する / 表示する / 描く(下段) |
| `f` | 焦点を移す / ファンクション(下段) |

### ティアの表記

このマニュアルでは、ティアを`aSdF`のように4文字で表記します。並び順は`a`/`s`/`d`/`f`で
固定されており、**大文字は「押している状態」、小文字は「押していない状態」**を表します。
設定ファイル`AbcAct.edn`でも同じ表記を使っているため、そのまま読み替えられます。

| 表記 | 押しているActキー |
|---|---|
| `asdf` | なし(左⌘だけ) |
| `Asdf` | `a` |
| `aSdF` | `s`と`f` |
| `ASDF` | `a`と`s`と`d`と`f` |

---

## 2. 基本キー(レイヤーに入らなくても効くもの)

| キー操作 | 動作 |
|---|---|
| Caps Lockを押しっぱなし | **⌘として機能する**(Asteriskレイヤー内を除く) |
| Caps Lockを単体でタップ(離すまで他のキーを押さない) | Esc |
| 左⌘を単体でタップ | 英語入力ソースに切り替え(US Extended配列) |
| 右⌘を単体でタップ | かなキー(`japanese_kana`)を送出 — 日本語入力のかな/英数モードを切り替え(入力ソースそのものの切り替えではありません) |
| 右⌘を押しっぱなし | 通常の⌘ |
| 左⌥を単体でタップ | ギリシャ語入力ソースに切り替え |
| `fn` | **⌃として機能する**(押し方によらず常に) |
| Spacebarを単体でタップ | 通常のスペース |
| Spacebarを押しっぱなし | **⇧として機能する**(左右どちらの親指からも届く唯一の修飾キー) |
| 左⇧を単体でタップ | `` ` ``(バッククォート) |
| 右⇧を押しながら左⇧を単体でタップ | `~`(チルダ) |
| 右⇧を単体でタップ | `\`(バックスラッシュ) |
| 左⇧を押しながら右⇧を単体でタップ | `\|`(パイプ) |

補足: 左右の⇧キーはBra/Cketレイヤーの起動に使うため、本来の⇧操作には
Spacebarを用います。同様に左⌘もAsteriskの起動に使うため、通常の⌘操作には
Caps Lockと右⌘を使います。⌥は左右とも通常どおり機能します
(左⌥を単体でタップした場合はギリシャ文字入力に切り替わります)。

### アクセント記号(Asterisk・Cketどちらのレイヤーにも入っていない時)

フランス語などのアクセント記号を入力するための下準備キーです。押した後に文字を続けて
打つと、é・è・ü・â・çのようなアクセント付き文字になります(macOS標準の仕組みです)。
`\`だけは例外で、下準備キーではなく単独で`œ`が出力されます。

| キー | 動作 |
|---|---|
| `[` | アクサンテギュの下準備(例: eに続けて押すと é) |
| `]` | アクサングラーブの下準備(例: eに続けて押すと è) |
| `\` | `œ`(合字)を入力(⌥Q)。下準備キーではなく、これ1つで確定します |
| ⇧`[` | アクサンシルコンフレックスの下準備(例: aに続けて押すと â) |
| ⇧`]` | ウムラウトの下準備(例: uに続けて押すと ü) |
| ⇧`\` | セディーユの下準備(例: cに続けて押すと ç) |

---

## 3. Asterisk(左⌘)レイヤー

### 3.1 アプリ起動(ホームポジション: q/w/e/r/t/y/g/a/s/d/f)

`a`は他のキーの動作を切り替える特殊なキーでもあります。`a`を追加で押しっぱなしにすると、
一部のキーで起動するアプリが変わります。

| キー | `asdf`(Actなし) | `Asdf`(`a`も押しながら) |
|---|---|---|
| `a`単体タップ | Raycastを起動 | ― |
| `s`単体タップ | Cometを起動 | Google Chromeを起動 |
| `d`単体タップ | MdoroMacを起動 | カレンダーを起動 |
| `f`単体タップ | Finderを起動 | iTermを起動 |
| `q` | Raycastのメモ機能を起動 | このマニュアルを開く(Raycast経由) |
| `w` | Writerを起動 | Obsidianを起動 |
| `e` | Cursorを起動 | Logseqを起動 |
| `r` | Claudeを起動 | ChatGPTを起動 |
| `t` | 選択中のテキストを日本語に翻訳(Raycast) | 選択中のテキストを英語に翻訳(Raycast) |
| `y` | DeepLを起動 | 辞書(Dictionary)を起動 |
| `g` | Slackを起動 | Spark Desktop(メール)を起動 |

補足: `s`/`d`/`f`には、押しっぱなしにして他のキーの動作を切り替える役割もあります。
たとえば`d`を押しながら他のキーを押している間は「削除系の操作(後述3.3)」に切り替わり、
`d`を押してすぐ離すと上記のアプリが起動します。

### 3.2 クリップボード履歴からペースト(Caps Lockキー)

親指(左⌘)と小指(Caps Lock)だけで、クリップボード履歴アプリ「Maccy」を開いて
そのままペーストできます。

1. 左⌘を押したまま、Caps Lockを押す → Maccyのポップアップが開く
2. 左⌘とCaps Lockを押したまま、数字キー(`1`〜`0`)またはアルファベットキー(`a`〜`z`)を押す
   → その番号/文字に対応するクリップボード履歴がペーストされる

### 3.3 ナビゲーション・編集(右手: h/j/k/l/u/i/o/p)

16ティアによって、右手側のキーの動作が変わります。

全16ティアにおいて、`h`は左、`l`は右の操作に対応します。`j`と`k`は常に互いに逆の操作が
対になっており、多くのティアでは下/上の操作に割り当てています(唯一の例外は`aSDf`の
日本語入力で、ここだけはmacOS側のキー割り当てに従っています)。`u`/`i`/`o`/`p`はティアに
よって動作が変わるため、下表で確認してください。

#### h/j/k/l(方向キー)

| ティア | h(左) | j(下) | k(上) | l(右) |
|---|---|---|---|---|
| `asdf` | ← | ↓ | ↑ | → |
| `Asdf` | ⌥←(単語単位で左へ) | ⌥↓(段落単位で下へ) | ⌥↑(段落単位で上へ) | ⌥→(単語単位で右へ) |
| `aSdf` | ⇧←(1文字選択) | ⇧↓(1行選択) | ⇧↑(1行選択) | ⇧→(1文字選択) |
| `ASdf` | ⇧⌥←(単語単位で選択) | ⇧⌥↓(段落単位で選択) | ⇧⌥↑(段落単位で選択) | ⇧⌥→(単語単位で選択) |
| `asDf` | 1文字削除(後退) | 下1行を削除 | 上1行を削除 | 1文字削除(前方) |
| `AsDf` | ⌥Delete(単語削除・後方) | 段落を選択して削除(下方向) | 段落を選択して削除(上方向) | ⌥Delete(単語削除・前方) |
| `aSDf` | 再変換(⌃⇧R) | ひらがなに変換(⌃J) | カタカナに変換(⌃K) | 半角英数に変換(⌃`;`) |
| `ASDf` | ⌘`[`(インデントを減らす) | ⌘Z(取り消し) | ⇧⌘Z(やり直し) | ⌘`]`(インデントを増やす) |
| `asdF` | マウスカーソルを左へ移動 | マウスカーソルを下へ移動 | マウスカーソルを上へ移動 | マウスカーソルを右へ移動 |
| `AsdF` | マウスカーソルを左へ移動(高速) | マウスカーソルを下へ移動(高速) | マウスカーソルを上へ移動(高速) | マウスカーソルを右へ移動(高速) |
| `aSdF` | ウィンドウを少し左へ移動 | ウィンドウを少し下へ移動 | ウィンドウを少し上へ移動 | ウィンドウを少し右へ移動 |
| `ASdF` | ウィンドウを画面左半分に(繰り返し押すと1/2→2/3→1/3で切り替わる) | ウィンドウを画面下半分に(同上) | ウィンドウを画面上半分に(同上) | ウィンドウを画面右半分に(同上) |
| `asDF` | ⇧⌃Tab(タブ切り替え・逆方向) | ⌘W(タブを閉じる) | ⇧⌘T(閉じたタブを復元) | ⌃Tab(タブ切り替え・順方向) |
| `AsDF` | ⇧⌘`` ` ``(同じアプリの前のウィンドウへ) | ⇧⌘W(ウィンドウを閉じる) | ⌘N(新規ウィンドウ) | ⌘`` ` ``(同じアプリの次のウィンドウへ) |
| `aSDF` | ⇧⌘Tab(アプリ切り替え・逆方向) | ⌘Q(アプリを終了) | ⌘`.`(切り替えをキャンセル) | ⌘Tab(アプリ切り替え・順方向) |
| `ASDF` | ⌃←(前のSpaceへ) | ⌃↓(このアプリの全ウィンドウを表示・App Exposé) | ⌃↑(Mission Control) | ⌃→(次のSpaceへ) |

#### u/i/o/p

| ティア | u | i | o | p |
|---|---|---|---|---|
| `asdf` | PgUp | Home(行頭へ) | End(行末へ) | PgDn |
| `Asdf` | ⌘↑(文書の先頭へ) | ⌘←(行の先頭へ) | ⌘→(行の末尾へ) | ⌘↓(文書の末尾へ) |
| `aSdf` | ⇧PgUp(選択) | ⇧Home(行頭まで選択) | ⇧End(行末まで選択) | ⇧PgDn(選択) |
| `ASdf` | ⇧⌘↑(文書先頭まで選択) | ⇧⌘←(行頭まで選択) | ⇧⌘→(行末まで選択) | ⇧⌘↓(文書末尾まで選択) |
| `asDf` | ⇧PgUp(1画面分を選択して削除) | ⇧Home(行頭まで選択して削除) | ⇧End(行末まで選択して削除) | ⇧PgDn(1画面分を選択して削除) |
| `AsDf` | 文書の先頭まで選択して削除 | 行の先頭まで選択して削除 | 行の末尾まで選択して削除 | 文書の末尾まで選択して削除 |
| `aSDf` | ⌘X(カット) | ⌘C(コピー) | ⌘V(ペースト) | ⇧⌘⌥V(書式を合わせてペースト) |
| `ASDf` | ⌥⇧↑(現在行を上に複製) | ⌥↑(現在行を上へ移動) | ⌥↓(現在行を下へ移動) | ⌥⇧↓(現在行を下に複製) |
| `asdF` | 横スクロール(左) | 縦スクロール(下) | 縦スクロール(上) | 横スクロール(右) |
| `AsdF` | 横スクロール・高速(左) | 縦スクロール・高速(下) | 縦スクロール・高速(上) | 横スクロール・高速(右) |
| `aSdF` | ウィンドウを最大化 | ウィンドウを少し縮小 | ウィンドウを少し拡大 | ウィンドウをほぼ最大化(画面の約70%) |
| `ASdF` | 前のSpaceへウィンドウを移動 | 前のディスプレイへウィンドウを移動 | 次のディスプレイへウィンドウを移動 | 次のSpaceへウィンドウを移動 |
| `asDF` | ⌘1(最初のタブへ) | ⌘`[`(戻る) | ⌘`]`(進む) | ⌘9(最後のタブへ) |
| `AsDF` | フルスクリーン表示を切り替え | ⇧⌃F6(前のパネルへフォーカス) | ⌃F6(フローティングウィンドウへフォーカス) | ⌘M(ウィンドウを最小化) |
| `aSDF` | ⌥⌘H(ほかのアプリを隠す) | ⌥⌘W(全ウィンドウを閉じる) | ⌃F3(Dockへフォーカス) | ⌘H(このアプリを隠す) |
| `ASDF` | レイアウト1(Raycastの自作ウィンドウレイアウト) | レイアウト2(未設定) | レイアウト3(未設定) | レイアウト4(未設定) |

### 3.4 補助キー(`[`/`]`/`;`/`'`)

`f`を押していないときは、`d`の押下状態だけで動作が決まります(`a`/`s`は影響しません)。

| ティア | `[` | `]` | `;` | `'` |
|---|---|---|---|---|
| `asdf` `Asdf` `aSdf` `ASdf` | Enter | ⇧Enter | Enter | ⇧Enter |
| `asDf` `AsDf` `aSDf` `ASDf` | 全選択(⌘A) | 全選択(⌘A) | 全選択(⌘A) | 全選択(⌘A) |

`f`を押しているときは、8ティアそれぞれに別の動作が割り当てられます。

| ティア | `[` | `]` | `;` | `'` |
|---|---|---|---|---|
| `asdF` | 左クリック | 右クリック | 左クリック | 右クリック |
| `AsdF` | 左クリック | 右クリック | 左クリック | 右クリック |
| `aSdF` | 画面左上6分の1に配置 | 画面右上6分の1に配置 | 画面左下6分の1に配置 | 画面右下6分の1に配置 |
| `ASdF` | 画面左上4分の1に配置 | 画面右上4分の1に配置 | 画面左下4分の1に配置 | 画面右下4分の1に配置 |
| `asDF` | 新規タブ(⌘T) | 同左 | 同左 | 同左 |
| `AsDF` | Switch Windows(Raycast) | 同左 | 同左 | 同左 |
| `aSDF` | Launchpad | 同左 | 同左 | 同左 |
| `ASDF` | ステータスメニューへフォーカス移動(⌃F8) | 同左 | 同左 | 同左 |

マウス操作の2ティアで左右クリックの割り当てが同じなのは、この2ティアにおける`a`の役割が
「移動量を倍にする」ことであり、クリック操作には倍にする量が存在しないためです。

タブ・ウィンドウ・アプリの3ティアには、それぞれの階層で「新しく作業を始める入口」を
割り当てています。単に新規作成するだけでなく、検索してから対象を決められる操作を
選んでいます。

Enterと⇧Enterは左列(`[`/`;`)と右列(`]`/`'`)で分けて配置しています。修飾キーを追加するの
ではなく物理的な位置で区別することで、送信と改行のように結果が大きく異なる操作を
押し間違えにくくしています(左右クリックも同じ考え方です)。`;`はホームポジションで
小指を置く最も押しやすいキーの一つなので、よく使う「全選択」をここにも割り当てています。

### 3.5 `\`キー(素のスペース)

Asteriskレイヤー内で`\`を押すとスペースが出力されます。Actキーの状態に関係なく、
16ティアすべてで同じ動作をする唯一のキーです。

Spacebar自体を⇧として使っているため、スペースの押しっぱなしや⇧付きのスペースを
入力する手段がなくなります。そうした入力の受け皿として、このキーを割り当てています。

### 3.6 下段キー(左⇧/z/x/c/v/b/n/m/`,`/`.`/`/`/右⇧)

#### 記号入力(`a`を押していない/押している状態のみ切り替わる)

| キー | `asdf`(Actなし) | `Asdf`(`a`も押しながら) |
|---|---|---|
| 左⇧ | `?` | 絵文字・記号検索(Raycast)を起動 |
| `z` | `!` | `^` |
| `x` | `@` | `&` |
| `c` | `#` | `*` |
| `v` | `$` | `(` |
| `b` | `%` | `)` |
| `n` | `-` | `_` |
| `m` | `=` | `+` |
| `,` | `[` | `{` |
| `.` | `]` | `}` |
| `/` | `:` | 特殊記号(«) |
| 右⇧ | `"` | 特殊記号(») |

#### クリップの保存・呼び出し(`s`/`d`を押しながら)

同じ12キー(左⇧/z/x/c/v/b/n/m/`,`/`.`/`/`/右⇧)が、0〜11番のクリップスロットに
対応しています。

- `s`を押しながらいずれかのキー: ⌘Cで選択範囲をコピーしてから、そのスロットに保存
- `d`を押しながらいずれかのキー: そのスロットの内容をクリップボードに戻してからペースト
  (このとき現在のクリップボードは上書きされます)

スロットの中身はディスク(`~/.cache/abcact/clips/`)に保存されるため、Maccyの履歴とは
独立しており、再起動しても残ります。

#### ファンクションキー(`f`を押しながら)

同じ12キーがF1〜F12に対応します(左⇧=F1、z=F2、x=F3…右⇧=F12)。

### 3.7 Caps Lock(気軽に呼べる操作)

Asterisk中(左⌘を押している間)は、Caps Lockにも`a`/`s`/`d`/`f`の押し方に応じた
動作が割り当てられています。

| ティア | Caps Lockの動作 |
|---|---|
| `asdf` | クリップボード履歴(Maccy)を開く — ペースト方法は3.2を参照 |
| `Asdf` | 音声入力を起動 |
| `aSdf` | スクリーンショット(範囲を選択してクリップボードへ・⇧⌃⌘4) |
| `asDf` | スクリーンショットと画面収録のパネルを表示(⇧⌘5) |
| `asdF` | クリップボードの画像をAirDropで送信 |

### 3.8 Escape(破壊的なシステムアクション)

Asterisk中は、Escapeキーにも`a`/`s`/`d`の押し方に応じてセッションを中断・終了する
操作が割り当てられています。Caps Lockの操作(3.7)が「気軽に呼べる操作」なのに対し、
Escapeは「取り消せない・実行頻度の低い操作」専用です。

| ティア | Escapeの動作 |
|---|---|
| `asdf` | 画面をロック(⌃⌘Q) |
| `Asdf` | スリープ |
| `aSdf` | 再起動 |
| `asDf` | ログアウト |

Asteriskに入っていない時のEscapeキーは、何も割り当てられていない通常のEscです。

### 3.9 Tab(フォーカスジャンプ)

Asterisk中は、Tabキーにも動作が割り当てられています。いずれの動作も、実行後
すぐに文字入力へ戻ることを前提にしているため、ホームポジションを維持できること
を重視しています。

| ティア | Tabの動作 |
|---|---|
| `asdf` | アクティブ/次のウィンドウへフォーカス移動(⌃F4) |
| `asdF` | メニューバーへフォーカス移動(⌃F2) |

⌃F4の移動先はウィンドウ、⌃F2の移動先はアプリのメニューです。Tabには
この隣り合う2ティアのみを割り当てています。ステータスメニュー(⌃F8)は前面のアプリが
何であっても変わらないシステム全体のものなので、最も広いティアである
`ASDF`の補助キーに移しました。

### 3.10 iTerm2の中での上書き

iTerm2が前面にあるときは、テキスト編集の8ティアに限り、macOSの編集キーではなく
シェル向けの制御文字を送信します。シェルには「選択」も「段落」もないため、
そのまま送ると機能しないか、意図しない結果になるためです。

動作が変わるのは下表に挙げたものだけで、挙げていないものは通常どおりです。

| ティア | 変わるキー | iTerm2での動作 |
|---|---|---|
| `asdf` | `i`/`o` | 行頭へ(⌃A)/行末へ(⌃E) |
| `Asdf` | `h`/`l` | 1単語戻る(⌥B)/1単語進む(⌥F) |
| `Asdf` | `j`/`k` | 履歴を前方検索(⌥N)/後方検索(⌥P) |
| `Asdf` | `u`/`i`/`o`/`p` | 履歴の先頭(⌥`<`)/行頭(⌃A)/行末(⌃E)/履歴の末尾(⌥`>`) |
| `asDf` | `i`/`o` | 行を消す(⌃U)/行末まで消す(⌃K) |
| `AsDf` | `h`/`l` | 1単語消す(⌃W)/1単語前方へ消す(⌥D) |
| `AsDf` | `i`/`o` | 行を消す(⌃U)/行末まで消す(⌃K) |
| `ASDf` | `j`/`k` | 取り消し(⌃`_`)/やり直し(⌥R) |

**無反応になるティアとキー**: `aSdf`と`ASdf`(選択2ティア)の8キーすべて、`asDf`と`AsDf`の
`j`/`k`/`u`/`p`、`aSDf`の`h`/`j`/`k`/`l`、`ASDf`の`h`/`l`/`u`/`i`/`o`/`p`。

補足: `Asdf`の`j`/`k`は、入力中の文字列で始まるコマンドを履歴から探します。
`git c`まで入力してから`k`を繰り返すと、`git c`で始まる過去のコマンドを遡れます。

補足: `⌥`を使う動作(単語移動・単語削除)には、iTerm2側で
**Settings → Profiles → Keys → Left Option Key を「Esc+」**にしておく必要があります。
この設定だけはリポジトリの外にあります。

補足: この切り替えはiTerm2にだけ適用されます。他のターミナルアプリや、
エディタに内蔵されたターミナルでは、この8ティアでも通常どおりmacOSの編集キーを送信します。

---

## 4. Bra(左⇧)レイヤー

左⇧を押しっぱなしにすると入ります。片手テンキー専用のレイヤーです。

| キー | 動作 |
|---|---|
| `z`/`x`/`c`/`v` | `0`/`1`/`2`/`3` |
| `s`/`d`/`f` | `4`/`5`/`6` |
| `w`/`e`/`r` | `7`/`8`/`9` |
| `a` | Backspace |
| `q` | 現在行を削除 |

---

## 5. Cket(右⇧)レイヤー

右⇧を押しっぱなしにすると入ります。メディア・ハードウェア操作専用です。

| キー | 動作 |
|---|---|
| `[` | 音量を下げる |
| `]` | 音量を上げる |
| `\` | ミュート |
| `;` | 画面の輝度を下げる |
| `'` | 画面の輝度を上げる |
| `.` | キーボードのバックライトを暗くする |
| `/` | キーボードのバックライトを明るくする |
| 右⌥を押しながら`[` | 早送り |
| 右⌥を押しながら`]` | 巻き戻し |
| 右⌥を押しながら`\` | 再生/一時停止 |

---

## 6. 補足

実装上の細かい制約(Karabiner/Gokuの仕様上の注意点)や、この配列に至るまでの
試行錯誤の記録は`NOTES.md`を参照してください。
