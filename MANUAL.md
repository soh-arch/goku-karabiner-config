# AbcAct 操作マニュアル

> **図解版（英語）**: <https://soh-arch.github.io/goku-karabiner-config/>
> 内容は同じですが、キーボード図・16ティアのインタラクティブな探索・全文検索がつきます。

このマニュアルは、Goku/Karabiner-Elementsの設定ファイル(`AbcAct.edn`)がどう動くかを、
技術的な仕組みを知らなくても使えるように、実際の操作と結果だけで説明したものです。

## 1. 全体の考え方

このキーボード配列には、3つの「特殊レイヤー」があります。特定のキーを押したまま
(または押しっぱなしにしたまま)別のキーを押すことで、普段と違う動作をさせます。

| レイヤー名 | 起動キー | 主な用途 |
|---|---|---|
| **Asterisk** | 左Command | アプリ起動・カーソル移動・編集・ウィンドウ管理など |
| **Bra** | 左Shift | 片手用の仮想テンキー |
| **Cket** | 右Shift | 音量・輝度などメディア操作 |

さらに**Asterisk**レイヤーの中には「Act」という第2段階の仕組みがあります。
左Commandを押したまま、ホームポジションの`a`・`s`・`d`・`f`キーを**追加で**押しっぱなしに
することで、他のキー(`h`/`j`/`k`/`l`など)の動作を切り替えられます。
`a`・`s`・`d`・`f`は同時に複数押すこともできるので、組み合わせは最大16通りになります。

このマニュアルでは、たとえば「左Commandを押しながら、さらに`s`と`f`を押しっぱなしにして、
`h`を押す」という状態を **`Cmd+S+F → h`** のように表記します。何も追加で押していない
(`a`/`s`/`d`/`f`のどれも押していない)状態は単に **`Cmd → h`** と表記します。

---

## 2. 基本キー(レイヤーに入らなくても効くもの)

| キー操作 | 動作 |
|---|---|
| Caps Lockを押しっぱなし | **Commandキーとして機能する**(Asteriskレイヤー内を除く) |
| Caps Lockを単体でタップ(離すまで他のキーを押さない) | Escキー |
| 左Commandを単体でタップ | 英語入力ソースに切り替え(US Extended配列) |
| 右Commandを単体でタップ | かなキー(`japanese_kana`)を送出 — 日本語入力のかな/英数モードを切り替え(入力ソースそのものの切り替えではありません) |
| 右Commandを押しっぱなし | 通常のCommandキー |
| 左Optionを単体でタップ | ギリシャ語入力ソースに切り替え |
| `fn` | **Controlキーとして機能する**(押し方によらず常に) |
| Spacebarを単体でタップ | 通常のスペース |
| Spacebarを押しっぱなし | **Shiftとして機能する**(左右どちらの親指からも届く唯一の修飾キー) |
| 左Shiftを単体でタップ | `` ` ``(バッククォート) |
| 右Shiftを押しながら左Shiftを単体でタップ | `~`(チルダ) |
| 右Shiftを単体でタップ | `\`(バックスラッシュ) |
| 左Shiftを押しながら右Shiftを単体でタップ | `\|`(パイプ) |

補足: 左右のShiftキーはBra/Cketレイヤーの起動に使われているため、Shift本来の役割は
Spacebarが担っています。同様にCommandは、左CommandがAsteriskの起動に使われて
いるため、Caps Lockと右Commandが担っています。Optionは左右とも通常どおりです
(左Optionは単体タップにギリシャ文字入力の切替が乗っています)。

### アクセント記号(Asterisk・Cketどちらのレイヤーにも入っていない時)

フランス語などのアクセント記号を入力するための下準備キーです。押した後に文字を続けて
打つと、é・è・ü・â・çのようなアクセント付き文字になります(macOS標準の仕組みです)。
`\`だけは例外で、下準備キーではなく`œ`が単独で出力されます。

| キー | 動作 |
|---|---|
| `[` | アクサンテギュの下準備(例: eに続けて押すと é) |
| `]` | アクサングラーブの下準備(例: eに続けて押すと è) |
| `\` | `œ`(合字)を入力(⌥Q)。下準備キーではなく、これ1つで確定します |
| Shift+`[` | アクサンシルコンフレックスの下準備(例: aに続けて押すと â) |
| Shift+`]` | ウムラウトの下準備(例: uに続けて押すと ü) |
| Shift+`\` | セディーユの下準備(例: cに続けて押すと ç) |

---

## 3. Asterisk(左Command)レイヤー

### 3.1 アプリ起動(ホームポジション: q/w/e/r/t/y/g/a/s/d/f)

`a`は他のキーの動作を切り替える特殊なキーでもあります。`a`を追加で押しっぱなしにすると、
一部のキーの起動先アプリが変わります。

| キー | `Cmd →` (通常) | `Cmd+A →` (aも押しながら) |
|---|---|---|
| `a`単体タップ | Raycastを起動 | ― |
| `s`単体タップ | Cometを起動 | Google Chromeを起動 |
| `d`単体タップ | MdoroMacを起動 | カレンダーを起動 |
| `f`単体タップ | Finderを起動 | iTermを起動 |
| `q` | Raycastのメモ機能を起動 | このマニュアルを開く(Raycast経由) |
| `w` | Writerを起動 | Obsidianを起動 |
| `e` | Cursorを起動 | Logseqを起動 |
| `r` | Claudeを起動 | ChatGPTを起動 |
| `t` | 選択中のテキストを日本語に翻訳(Raycast) | 選択中のテキストを英語に翻訳(Raycast) |
| `y` | DeepLを起動 | 辞書(Dictionary)を起動 |
| `g` | Slackを起動 | Spark Desktop(メール)を起動 |

補足: `s`/`d`/`f`は「押しっぱなしで意味を切り替える」役割も持っています。たとえば
`d`を押しながら他のキーを押している間は「削除系の操作(後述3.3)」に切り替わっており、
`d`を押してすぐ離すと上記のアプリが起動します。

### 3.2 クリップボード履歴からペースト(Caps Lockキー)

親指(左Command)と小指(Caps Lock)だけで、クリップボード履歴アプリ「Maccy」を開いて
そのままペーストできます。

1. 左Commandを押したまま、Caps Lockを押す → Maccyのポップアップが開く
2. 左CommandとCaps Lockを押したまま、数字キー(`1`〜`0`)またはアルファベットキー(`a`〜`z`)を押す
   → その番号/文字に対応するクリップボード履歴がペーストされる

### 3.3 ナビゲーション・編集(右手: h/j/k/l/u/i/o/p)

`a`・`s`・`d`・`f`の押し方(16通り)によって、右手側のキーの動作が変わります。
`h`/`l`は全16行で左/右を担当します。`j`/`k`は常に互いに逆の意味を持つ対で、多くの行
ではそれが下/上として現れます(唯一の例外は`s`+`d`の日本語入力の行で、ここだけは
macOS側がキーの割当を決めています)。

`u`/`i`/`o`/`p`は行によって役割が変わります。カーソル移動・選択の行(なし・`a`・`s`・
`a`+`s`)では境界(ページ上/行頭/行末/ページ下と、その増幅版)を担当し、削除系の行
(`d`・`a`+`d`)ではその境界を使った削除になります。スクロールの行(`f`・`a`+`f`)では
`h`/`j`/`k`/`l`と同じ左/下/上/右をスクロールで繰り返します。配置の行(`s`+`f`・
`a`+`s`+`f`)では、`h`/`j`/`k`/`l`が画面の中での位置を決め、`u`/`i`/`o`/`p`が
大きさ(`s`+`f`)または画面をまたぐ移動(`a`+`s`+`f`)を担当します。それ以外の行
(`s`+`d`のカット/コピー/ペースト、`a`+`s`+`d`の行複製/移動、タブ/ウィンドウ/アプリ
操作など)では4キー共通の方向の意味を持たず、行ごとに異なる操作へ転用されています。
各行の実際の意味は下表のセルの表記を見てください。

#### h・j・k・l(方向キー)

| 追加で押すキー | h(左) | j(下) | k(上) | l(右) |
|---|---|---|---|---|
| なし | ← | ↓ | ↑ | → |
| `a` | Option+← (単語単位で左へ) | Option+↓ (段落単位で下へ) | Option+↑ (段落単位で上へ) | Option+→ (単語単位で右へ) |
| `s` | Shift+← (1文字選択) | Shift+↓ (1行選択) | Shift+↑ (1行選択) | Shift+→ (1文字選択) |
| `a`+`s` | Shift+Option+← (単語単位で選択) | Shift+Option+↓ (段落単位で選択) | Shift+Option+↑ (段落単位で選択) | Shift+Option+→ (単語単位で選択) |
| `d` | 1文字削除(後退) | 下1行を削除 | 上1行を削除 | 1文字削除(前方) |
| `a`+`d` | Option+Delete (単語削除・後方) | 段落を選択して削除(下方向) | 段落を選択して削除(上方向) | Option+Delete (単語削除・前方) |
| `s`+`d` | 再変換(Ctrl+Shift+R) | ひらがなに変換(Ctrl+J) | カタカナに変換(Ctrl+K) | 半角英数に変換(Ctrl+`;`) |
| `a`+`s`+`d` | Command+`[` (インデントを減らす) | Command+Z (取り消し) | Shift+Command+Z (やり直し) | Command+`]` (インデントを増やす) |
| `f` | マウスカーソルを左へ移動 | マウスカーソルを下へ移動 | マウスカーソルを上へ移動 | マウスカーソルを右へ移動 |
| `a`+`f` | マウスカーソルを左へ移動(高速) | マウスカーソルを下へ移動(高速) | マウスカーソルを上へ移動(高速) | マウスカーソルを右へ移動(高速) |
| `s`+`f` | ウィンドウを少し左へ移動 | ウィンドウを少し下へ移動 | ウィンドウを少し上へ移動 | ウィンドウを少し右へ移動 |
| `a`+`s`+`f` | ウィンドウを画面左半分に(繰り返し押すと1/2→2/3→1/3で切り替わる) | ウィンドウを画面下半分に(同上) | ウィンドウを画面上半分に(同上) | ウィンドウを画面右半分に(同上) |
| `d`+`f` | Shift+Ctrl+Tab (タブ切り替え・逆方向) | Command+W (タブを閉じる) | Shift+Command+T (閉じたタブを復元) | Ctrl+Tab (タブ切り替え・順方向) |
| `a`+`d`+`f` | Shift+Command+`` ` `` (同じアプリの前のウィンドウへ) | Shift+Command+W (ウィンドウを閉じる) | Command+N (新規ウィンドウ) | Command+`` ` `` (同じアプリの次のウィンドウへ) |
| `s`+`d`+`f` | Shift+Command+Tab (アプリ切り替え・逆方向) | Command+Q (アプリを終了) | `.` (切り替えをキャンセル) | Command+Tab (アプリ切り替え・順方向) |
| `a`+`s`+`d`+`f` | Ctrl+← (前のSpaceへ) | Ctrl+↓ (このアプリの全ウィンドウを表示・App Exposé) | Ctrl+↑ (Mission Control) | Ctrl+→ (次のSpaceへ) |

#### u・i・o・p

| 追加で押すキー | u | i | o | p |
|---|---|---|---|---|
| なし | Page Up | Home(行頭へ) | End(行末へ) | Page Down |
| `a` | Command+↑ (文書の先頭へ) | Command+← (行の先頭へ) | Command+→ (行の末尾へ) | Command+↓ (文書の末尾へ) |
| `s` | Shift+Page Up (選択) | Shift+Home (行頭まで選択) | Shift+End (行末まで選択) | Shift+Page Down (選択) |
| `a`+`s` | Shift+Command+↑ (文書先頭まで選択) | Shift+Command+← (行頭まで選択) | Shift+Command+→ (行末まで選択) | Shift+Command+↓ (文書末尾まで選択) |
| `d` | Shift+Page Up (1画面分を選択して削除) | Shift+Home (行頭まで選択して削除) | Shift+End (行末まで選択して削除) | Shift+Page Down (1画面分を選択して削除) |
| `a`+`d` | 文書の先頭まで選択して削除 | 行の先頭まで選択して削除 | 行の末尾まで選択して削除 | 文書の末尾まで選択して削除 |
| `s`+`d` | Command+X (カット) | Command+C (コピー) | Command+V (ペースト) | Shift+Command+Option+V (書式を合わせてペースト) |
| `a`+`s`+`d` | Option+Shift+↑ (現在行を上に複製) | Option+↑ (現在行を上へ移動) | Option+↓ (現在行を下へ移動) | Option+Shift+↓ (現在行を下に複製) |
| `f` | 横スクロール(左) | 縦スクロール(下) | 縦スクロール(上) | 横スクロール(右) |
| `a`+`f` | 横スクロール・高速(左) | 縦スクロール・高速(下) | 縦スクロール・高速(上) | 横スクロール・高速(右) |
| `s`+`f` | ウィンドウを最大化 | ウィンドウを少し縮小 | ウィンドウを少し拡大 | ウィンドウをほぼ最大化(画面の約70%) |
| `a`+`s`+`f` | 前のSpaceへウィンドウを移動 | 前のディスプレイへウィンドウを移動 | 次のディスプレイへウィンドウを移動 | 次のSpaceへウィンドウを移動 |
| `d`+`f` | Command+1 (最初のタブへ) | Command+`[` (戻る) | Command+`]` (進む) | Command+9 (最後のタブへ) |
| `a`+`d`+`f` | フルスクリーン表示を切り替え | Shift+Ctrl+F6 (前のパネルへフォーカス) | Ctrl+F6 (フローティングウィンドウへフォーカス) | Command+M (ウィンドウを最小化) |
| `s`+`d`+`f` | Option+Command+H (ほかのアプリを隠す) | Option+Command+W (全ウィンドウを閉じる) | Ctrl+F3 (Dockへフォーカス) | Command+H (このアプリを隠す) |
| `a`+`s`+`d`+`f` | レイアウト1 (Raycastの自作ウィンドウレイアウト) | レイアウト2(未設定) | レイアウト3(未設定) | レイアウト4(未設定) |

### 3.4 補助キー(`[`・`]`・`;`・`'`)

`f`を押していないときは、`d`の押下状態だけで動作が決まります(`a`・`s`は影響しません)。

| 追加で押すキー | `[` | `]` | `;` | `'` |
|---|---|---|---|---|
| なし(または`a`・`s`のみ) | Enter | Shift+Enter | Enter | Shift+Enter |
| `d`(または`a`+`d`・`s`+`d`・`a`+`s`+`d`) | 全選択(Command+A) | 全選択(Command+A) | 全選択(Command+A) | 全選択(Command+A) |

`f`を押しているときは、8つの段それぞれに別の動作が入ります。

| 追加で押すキー | `[` | `]` | `;` | `'` |
|---|---|---|---|---|
| `f` | 左クリック | 右クリック | 左クリック | 右クリック |
| `a`+`f` | 左クリック | 右クリック | 左クリック | 右クリック |
| `s`+`f` | 画面左上6分の1に配置 | 画面右上6分の1に配置 | 画面左下6分の1に配置 | 画面右下6分の1に配置 |
| `a`+`s`+`f` | 画面左上4分の1に配置 | 画面右上4分の1に配置 | 画面左下4分の1に配置 | 画面右下4分の1に配置 |
| `d`+`f` | 新規タブ(Command+T) | 同左 | 同左 | 同左 |
| `a`+`d`+`f` | Switch Windows(Raycast) | 同左 | 同左 | 同左 |
| `s`+`d`+`f` | Launchpad | 同左 | 同左 | 同左 |
| `a`+`s`+`d`+`f` | ステータスメニューへフォーカス移動(⌃F8) | 同左 | 同左 | 同左 |

マウスの2段で左右クリックが同じなのは、この2段の`a`が「移動量を倍にする」働きをしていて、
クリックには倍にする量がないからです。

タブ・ウィンドウ・アプリの3段には、それぞれの階層で「新しく始めるときの入口」が入っています。
ただ新しく生やすのではなく、検索してから決められるものを選んでいます。

`;`(セミコロン)はホームポジションで小指が置かれている、最も押しやすいキーの一つなので、
よく使う「全選択」をここにも割り当てています。

Enter/Shift+Enterは左列(`[`・`;`)と右列(`]`・`'`)で分けています。修飾キーを足すのではなく
物理的な位置で区別することで、送信と改行のように結果が大きく異なる操作を押し間違えにくく
しています(下段の左右クリックと同じ考え方です)。

### 3.5 `\`キー(素のスペース)

Asteriskレイヤー内で`\`を押すとスペースが出力されます。Actキーの状態に関係なく、
16ティアすべてで同じ動作をする唯一のキーです。

Spacebar自体はShiftになっているため、押しっぱなしのスペースやShift付きのスペースを
出す手段がなくなります。その受け皿がこのキーです。

### 3.6 下段キー(左Shift・z・x・c・v・b・n・m・,・.・/・右Shift)

#### 記号入力(`a`を押していない/押している状態のみ切り替わる)

| キー | `Cmd →`(通常) | `Cmd+A →`(aも押しながら) |
|---|---|---|
| 左Shift | `?` | 絵文字・記号検索(Raycast)を起動 |
| `z` | `!` | `^` |
| `x` | `@` | `&` |
| `c` | `#` | `*` |
| `v` | `$` | `(` |
| `b` | `%` | `)` |
| `n` | `-` | `_` |
| `m` | `=` | `+` |
| `,` | `[` | `{` |
| `.` | `]` | `}` |
| `/` | `:` | 特殊記号(«) |
| 右Shift | `"` | 特殊記号(») |

#### クリップ保存・呼び出し(`s`/`d`を押しながら)

同じ12キー(左Shift・z・x・c・v・b・n・m・,・.・/・右Shift)が、0〜11番のクリップボード
スロットに対応しています。

- `Cmd+S`を押しながらいずれかのキー: Command+Cで選択範囲をコピーしてから、そのスロットに保存
- `Cmd+D`を押しながらいずれかのキー: そのスロットの内容をクリップボードに戻してからペースト
  (このとき現在のクリップボードは上書きされます)

スロットの中身はディスク(`~/.cache/abcact/clips/`)に保存されるため、Maccyの履歴とは
独立していて、再起動しても残ります。

#### ファンクションキー(`f`を押しながら)

同じ12キーが F1〜F12 に対応します(左Shift=F1、z=F2、x=F3…右Shift=F12)。

### 3.7 Caps Lock

Asterisk中(左Commandを押している間)は、Caps Lockにも`a`/`s`/`d`/`f`の押し方に応じた
動作が割り当てられています。

| 追加で押すキー | Caps Lockの動作 |
|---|---|
| なし | クリップボード履歴(Maccy)を開く — ペースト方法は3.2を参照 |
| `a` | 音声入力を起動 |
| `s` | スクリーンショット(範囲を選択してクリップボードへ・⇧⌃⌘4) |
| `d` | スクリーンショットと画面収録のパネルを表示(⇧⌘5) |
| `f` | クリップボードの画像をAirDropで送信 |

### 3.8 Escape(破壊的なシステムアクション)

Asterisk中は、Escapeキーにも`a`/`s`/`d`の押し方に応じてセッションを終わらせる系の
操作が割り当てられています。Caps Lockの操作(3.7)が「気軽に呼べる操作」なのに対し、
Escapeは「取り消せない・実行頻度の低い操作」専用です。

| 追加で押すキー | Escapeの動作 |
|---|---|
| なし | 画面をロック(⌃⌘Q) |
| `a` | スリープ |
| `s` | 再起動 |
| `d` | ログアウト |

Asteriskに入っていない時のEscapeキーは、何も割り当てられていない通常のEscです。

### 3.9 Tab(フォーカスジャンプ)

Asterisk中は、Tabキーにも動作が割り当てられています。Tabは`a`と同じ左小指で
押すキーのため、`a`を同時に押すことができません。いずれの動作も、実行後すぐに
文字入力へ戻ることを前提にしているため、ホームポジションを維持できることを重視
しています。

| 追加で押すキー | Tabの動作 |
|---|---|
| なし | アクティブ/次のウィンドウへフォーカス移動(⌃F4) |
| `f` | メニューバーへフォーカス移動(⌃F2) |

⌃F4が向かうのはウィンドウ、⌃F2が向かうのはアプリのメニューです。Tabには
この隣り合う2段だけを置いています。ステータスメニュー(⌃F8)は前面のアプリが
何であっても変わらないシステム全体のものなので、最も広い段である
`a`+`s`+`d`+`f`の補助キーに移しました。

### 3.10 iTerm2の中では動作が変わります

iTerm2が前面のとき、テキスト編集の8段だけ、macOSの編集キーではなく
シェル向けの制御文字を送ります。シェルには「選択」も「段落」も無いため、
そのまま送ると効かないか、意図しない結果になるためです。

変わるのは下の表のセルだけで、書かれていないセルは通常どおりです。

| 追加で押すキー | 変わるキー | iTerm2での動作 |
|---|---|---|
| なし | `i`/`o` | 行頭へ(⌃A)/行末へ(⌃E) |
| `a` | `h`/`l` | 1単語戻る(⌥B)/1単語進む(⌥F) |
| `a` | `j`/`k` | 履歴を前方検索(⌥N)/後方検索(⌥P) |
| `a` | `u`/`i`/`o`/`p` | 履歴の先頭(⌥`<`)/行頭(⌃A)/行末(⌃E)/履歴の末尾(⌥`>`) |
| `d` | `i`/`o` | 行を消す(⌃U)/行末まで消す(⌃K) |
| `a`+`d` | `h`/`l` | 1単語消す(⌃W)/1単語前方へ消す(⌥D) |
| `a`+`d` | `i`/`o` | 行を消す(⌃U)/行末まで消す(⌃K) |
| `a`+`s`+`d` | `j`/`k` | 取り消し(⌃`_`)/やり直し(⌥R) |

**無反応になるセル**: `s`と`a`+`s`(選択2段)の8キーすべて、`d`と`a`+`d`の
`j`/`k`/`u`/`p`、`s`+`d`の`h`/`j`/`k`/`l`、`a`+`s`+`d`の`h`/`l`/`u`/`i`/`o`/`p`。

補足: `a`の`j`/`k`は、打ちかけの文字で始まるコマンドを履歴から探します。
`git c`まで打ってから`k`を繰り返すと、`git c`で始まる過去のコマンドを遡れます。

補足: `⌥`を使う動作(単語移動・単語削除)には、iTerm2側で
**Settings → Profiles → Keys → Left Option Key を「Esc+」**にしておく必要があります。
この設定だけはリポジトリの外にあります。

補足: この切り替えはiTerm2にだけ適用されます。他のターミナルアプリや、
エディタに内蔵されたターミナルでは、8段は通常どおりmacOSの編集キーを送ります。

---

## 4. Bra(左Shift)レイヤー

左Shiftを押しっぱなしにすると入ります。片手テンキー専用のレイヤーです。

| キー | 動作 |
|---|---|
| `z`/`x`/`c`/`v` | `0`/`1`/`2`/`3` |
| `s`/`d`/`f` | `4`/`5`/`6` |
| `w`/`e`/`r` | `7`/`8`/`9` |
| `a` | Backspace |
| `q` | 現在行を削除 |

---

## 5. Cket(右Shift)レイヤー

右Shiftを押しっぱなしにすると入ります。メディア・ハードウェア操作専用です。

| キー | 動作 |
|---|---|
| `[` | 音量を下げる |
| `]` | 音量を上げる |
| `\` | ミュート |
| `;` | 画面の輝度を下げる |
| `'` | 画面の輝度を上げる |
| `.` | キーボードのバックライトを暗くする |
| `/` | キーボードのバックライトを明るくする |
| 右Optionを押しながら`[` | 早送り |
| 右Optionを押しながら`]` | 巻き戻し |
| 右Optionを押しながら`\` | 再生/一時停止 |

---

## 6. 補足

実装上の細かい制約(Karabiner/Gokuの仕様上の注意点)や、この配列に至るまでの
試行錯誤の記録は`NOTES.md`を参照してください。
